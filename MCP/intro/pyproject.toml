[project]
name = "intro"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.4.1",
    "tavily-python>=0.5.1",
    "bs4>=0.0.2",
    "anthropic>=0.49.0",
    "python-dotenv>=1.0.1",
    "openai>=1.66.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
